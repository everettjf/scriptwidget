// widget-param: name,MM-DD (optional), e.g. Alex,12-18
const p = ($getenv("widget-param") || "Alex,12-18").split(","); const name=p[0].trim(); const md=(p[1]||"12-18").split("-").map(Number); const now=new Date(); let next=new Date(now.getFullYear(),md[0]-1,md[1]); if(next<now) next=new Date(now.getFullYear()+1,md[0]-1,md[1]); const days=Math.ceil((next-now)/86400000);
$render(<vstack frame="max" spacing="7" padding="14" background="#581c47"><hstack frame="max"><icon systemName="birthday.cake.fill" size="22" color="#f9a8d4"/><spacer/><text font="caption" color="#fbcfe8">{name}</text></hstack><spacer/><text font="largeTitle" weight="bold" color="white">{days}</text><text font="callout" color="#fce7f3">days until the celebration</text><spacer/></vstack>);
