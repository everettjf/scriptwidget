// widget-param: saved,target (optional), e.g. 1250,3000
const values = ($getenv("widget-param") || "1250,3000").split(",").map(Number); const saved = values[0] || 0; const target = values[1] || 3000;
$render(<vstack frame="max" alignment="leading" spacing="9" padding="14" background="#0f766e"><hstack frame="max"><text font="headline" weight="bold" color="white">Savings</text><spacer/><icon systemName="banknote.fill" size="20" color="#ccfbf1"/></hstack><text font="title2" weight="bold" color="white">${saved.toLocaleString()}</text><progress value={saved} total={target} color="#5eead4"/><text font="caption" color="#ccfbf1">{Math.round(saved/target*100)}% of ${target.toLocaleString()} goal</text></vstack>);
